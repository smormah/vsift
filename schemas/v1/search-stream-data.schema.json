{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vsift.dev/schemas/v1/search-stream-data.schema.json",
  "title": "VSift search JSON Lines terminal data v1",
  "description": "The data member of the complete or partial terminal result that ends a search --events jsonl stream. It is the page of search-data.schema.json without its items: the matching segments were the preceding evidence events (record_type transcript_segment, the same records transcript.get streams), in rank order, and record_count says how many there were. hits names each streamed segment and how it matched, in stream order. Pass next_cursor back with the same session, query, range and revision to stream the next page; null means the search is exhausted.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "session_id",
    "revision",
    "query",
    "range",
    "hits",
    "record_count",
    "next_cursor",
    "transcript_coverage"
  ],
  "properties": {
    "session_id": { "type": "string", "pattern": "^ses_[a-z0-9]{16,64}$" },
    "revision": { "$ref": "transcript-revision.schema.json" },
    "query": { "$ref": "search-data.schema.json#/$defs/query" },
    "range": {
      "oneOf": [
        { "type": "null" },
        { "$ref": "search-data.schema.json#/$defs/range" }
      ]
    },
    "hits": {
      "type": "array",
      "maxItems": 100,
      "items": { "$ref": "search-data.schema.json#/$defs/hit" }
    },
    "record_count": { "type": "integer", "minimum": 0, "maximum": 100 },
    "next_cursor": { "$ref": "search-data.schema.json#/$defs/next_cursor" },
    "transcript_coverage": { "$ref": "search-data.schema.json#/$defs/transcript_coverage" }
  }
}
